import styled from '@emotion/styled';

export const PaginationContainer = styled.div({
  borderWidth: '2px 0 0',
  borderStyle: 'solid',
  borderColor: '#e5e5e5',
  backgroundColor: 'white',
  textAlign: 'center',
  padding: '12px 10px',
});

// #a7d2cd color for disabled button text
export const PreviousButton = styled.div({

});

export const PreviousButtonDis = styled.div({

});

export const NextButton = styled.div({

});

export const NextButtonDis = styled.div({

});

export const PageNumContainer = styled.div({
  color: '#078171',
  display: 'inline-block',
  paddingTop: '3px',
});

export const PageNum = styled.span({
  lineHeight: '40px',
  height: '40px',
  width: '40px',
  fontSize: '14px',
  fontWeight: '700',
  border: 'none',
  display: 'inline-block',
  cursor: 'pointer',
  borderRadius: '2px',
  textAlign: 'center',
});

export const SelectedPageNum = styled.div({

});
